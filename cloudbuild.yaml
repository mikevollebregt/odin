# cloudbuild.yaml

steps:
  # Install Node.js and npm
  - name: 'node:14'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        apt-get update && apt-get install -y nodejs npm
      
  # Step 2: Use the official .NET SDK image
  - name: 'mcr.microsoft.com/dotnet/sdk:6.0'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        dotnet restore
        dotnet build --configuration Release
        dotnet publish -c Release -o /workspace/app

  # Step 3: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/core_backend:$COMMIT_SHA'
      - '.'

  # Step 4: Push the Docker image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/core_backend:$COMMIT_SHA'

  # Step 5: Deploy the application (example: deploy to Cloud Run)
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'core_backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/core_backend:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'

images:
  - 'gcr.io/$PROJECT_ID/core_backend:$COMMIT_SHA'
